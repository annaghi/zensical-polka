# Zensical Polka

## Development

Init or reset dev environment, served on [localhost:8000](http://localhost:8000)

```shell
make init
```

```shell
make reset
```

Start Zensical's dev server

```shell
make serve
```

Compile Rust crate

```shell
make compile
```

## Build site

```shell
make build
```
